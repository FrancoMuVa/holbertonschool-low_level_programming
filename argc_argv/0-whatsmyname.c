#include <stdio.h>

/**
 * main - Print its name.
 * @argc: Integer.
 * @argv: Array containg the program line arguments.
 *
 * Return: 0.
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
